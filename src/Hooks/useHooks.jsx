import React, { useEffect, useState } from 'react';

const useHooks = () => {
    const [itemHooks, setItemHooks] = useState([]);
    useEffect(() => {

    }, []);


    
    return (
        <div>
            
        </div>
    );
};

export default useHooks;
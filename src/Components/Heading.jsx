import React from 'react';

const Heading = ({title}) => {
    return (
        <div className='text-5xl leading-tight text-center font-bold'>
            <h1>{title}</h1>
            
        </div>
    );
};

export default Heading;